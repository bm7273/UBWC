# The API server. The front end (web/) is plain static files — no build step,
# no JS dependencies.
fastapi>=0.115
uvicorn>=0.30
# Only needed to (re)build kit.db from data/Kit Inventory.xlsx via migrate.py.
openpyxl>=3.1
